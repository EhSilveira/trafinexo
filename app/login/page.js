"use client";
import {useState} from "react";
import {signIn} from "../../lib/infotecApi";

export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);
async function submit(e){e.preventDefault();setLoading(true);setError("");try{await signIn(email,password);window.location.href="/dashboard"}catch(err){setError(err.message)}finally{setLoading(false)}}
return <main className="auth-page"><a className="auth-back" href="/">← Voltar para apresentação</a><section className="auth-card"><div className="auth-brand"><span>T</span><div><strong>TRAFINEXO</strong><small>by INFOTEC</small></div></div><p className="auth-kicker">ACESSO À PLATAFORMA</p><h1>Bem-vindo de volta.</h1><p className="auth-lead">Entre para acompanhar clientes, campanhas, testes e prioridades da sua operação.</p><form onSubmit={submit}><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@empresa.com" autoComplete="email"/></label><label>Senha<input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Sua senha" autoComplete="current-password"/></label>{error&&<div className="form-error">{error}</div>}<button className="btn auth-submit" disabled={loading}>{loading?"Entrando…":"Entrar no Trafinexo"}</button></form><p className="auth-switch">Ainda não tem acesso? <a href="/cadastro">Comece 15 dias grátis</a></p><div className="auth-security">🔒 Acesso protegido · dados isolados por organização</div></section></main>}
