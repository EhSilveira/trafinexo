"use client";
import {useState} from "react";
import {requestPasswordReset,signIn} from "../../lib/infotecApi";

export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);const[recovering,setRecovering]=useState(false);
async function submit(e){e.preventDefault();setLoading(true);setError("");setMessage("");try{await signIn(email,password);window.location.href="/dashboard"}catch(err){setError(err.message)}finally{setLoading(false)}}
async function recover(){if(!email){setError("Digite seu e-mail acima para receber as instruções de recuperação.");return}setRecovering(true);setError("");try{await requestPasswordReset(email);setMessage("Se existir uma conta com esse e-mail, as instruções de recuperação foram enviadas.")}catch(err){setError(err.message)}finally{setRecovering(false)}}
return <main className="auth-page"><a className="auth-back" href="/">← Voltar para apresentação</a><section className="auth-card"><div className="auth-brand"><span>T</span><div><strong>TRAFINEXO</strong><small>by INFOTEC</small></div></div><p className="auth-kicker">ACESSO À PLATAFORMA</p><h1>Bem-vindo de volta.</h1><p className="auth-lead">Entre para acompanhar clientes, campanhas, testes e prioridades da sua operação.</p><form onSubmit={submit}><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@empresa.com" autoComplete="email"/></label><label>Senha<input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Sua senha" autoComplete="current-password"/></label>{error&&<div className="form-error">{error}</div>}{message&&<div className="form-success">{message}</div>}<button className="btn auth-submit" disabled={loading}>{loading?"Entrando…":"Entrar no Trafinexo"}</button><button type="button" className="auth-link-button" onClick={recover} disabled={recovering}>{recovering?"Enviando…":"Esqueci minha senha"}</button></form><p className="auth-switch">Ainda não tem acesso? <a href="/cadastro">Comece 15 dias grátis</a></p><div className="auth-security">🔒 Após 6 tentativas incorretas, o acesso é temporariamente bloqueado e a recuperação é iniciada.</div></section></main>}
