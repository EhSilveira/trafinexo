"use client";
import {useState} from "react";
import {signUp} from "../../lib/infotecApi";

export default function Cadastro(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);
async function submit(e){e.preventDefault();setLoading(true);setError("");setMessage("");try{const data=await signUp({name,email,password});if(data?.access_token)window.location.href="/dashboard";else setMessage("Conta criada. Confirme seu e-mail para liberar o acesso e iniciar os 15 dias grátis.")}catch(err){setError(err.message)}finally{setLoading(false)}}
return <main className="auth-page auth-signup"><a className="auth-back" href="/">← Voltar para apresentação</a><section className="auth-card"><div className="trial-badge">15 DIAS GRÁTIS · SEM CARTÃO</div><div className="auth-brand"><span>T</span><div><strong>TRAFINEXO</strong><small>by INFOTEC</small></div></div><h1>Organize sua operação de tráfego em um só lugar.</h1><p className="auth-lead">Crie seu workspace. O período de teste começa quando sua conta é criada.</p><form onSubmit={submit}><label>Seu nome<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Nome do gestor" autoComplete="name"/></label><label>E-mail profissional<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@empresa.com" autoComplete="email"/></label><label>Crie uma senha<input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mínimo de 8 caracteres" autoComplete="new-password"/></label>{error&&<div className="form-error">{error}</div>}{message&&<div className="form-success">{message}</div>}<button className="btn auth-submit" disabled={loading}>{loading?"Criando seu workspace…":"Começar teste grátis"}</button></form><div className="trial-list"><span>✓ Carteira de clientes</span><span>✓ Campanhas e performance</span><span>✓ Testes A/B e criativos</span><span>✓ Sugestões e IA</span></div><p className="auth-switch">Já possui conta? <a href="/login">Entrar</a></p></section></main>}
